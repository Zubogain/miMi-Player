export interface ITrackState {
    name: string,
    path: string,
    isPlay: boolean,
    isPause: boolean,
    position: string,
    duration: string,
    positionMs: number,
    durationMs: number,
    precent: number,
    volume: number,
    volumeMuted: boolean
}